import { LearningWayStringPipe } from './learning-way-string.pipe';

describe('LearningWayStringPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningWayStringPipe();
    expect(pipe).toBeTruthy();
  });
});
